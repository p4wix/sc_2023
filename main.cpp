#include <fstream>

#include "Simulator/Simulator.h"
#include "Generator/SeedGenerator.h"

int main() {
	// auto* network = new Network();
	// auto simulator = Simulator(network);
	// simulator.run(50);

	// generateSeedsData();

	return 0;
}